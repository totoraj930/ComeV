{
  "name": "come-v-html5-viewer",
  "version": "1.0.0",
  "description": "ComeVの外部連携APIを使って配信画面にコメントを流すやつ",
  "author": "Reona Oshima(totoraj)",
  "license": "MIT",
  "private": true,
  "source": [
    "src/come_view.html"
  ],
  "scripts": {
    "dev": "parcel serve --no-cache",
    "build": "parcel build src/come_view.html --no-source-maps --no-cache --no-optimize --dist-dir ../src-tauri/assets/ --public-url ./"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.6.0",
    "parcel": "^2.6.0"
  }
}
